# 茶會小遊戲系統
## 事前準備
1. 測試學校網路環境有無（AP Isolation）決定如何使用系統
2. 測試題目是否順利顯示
3. 建議事先閱讀主介面附帶的遊戲規則說明
## 系統使用指南
### 方案 A：一般內網連線（首選）

適用於學校網路允許裝置互連時，操作最靈活。
1. **啟動伺服器**：打開伺服器程式，確保主機已連線至學校網路。
2. **確認 IP 位址**：在伺服器程式中確認目前的內部 IP（例如：`192.168.x.x`）。
3. **大螢幕投放**：使用教室電腦連線至遊戲主頁 `http://[您的IP]`，並投影至大螢幕。
4. **手機行動主持**：主持人手機連線至同一個 Wi-Fi，進入 `http://[您的IP]/GameAnswer` 查看答案。
> **💡 小撇步：** 如果手機連不上，請檢查主機的「防火牆」是否已關閉，或是否允許該埠口（Port）通過。


---

### 方案 B：若學校網路阻擋互連（備案）

若學校網路設有隔離（AP Isolation），導致手機連不上電腦時使用。

1. **單機運行**：直接在教室電腦開啟伺服器程式。
2. **設定延伸螢幕**：按下 `Win + P` 選擇 **「延伸」** 模式。
* **投影機/大螢幕**：拖曳瀏覽器視窗至此，顯示遊戲主網頁（`http://127.0.0.1`）。
* **講台螢幕**：開啟另一個分頁或瀏覽器，連線至 `http://127.0.0.1/GameAnswer` 供主持人查看答案。


3. **視窗配置**：確保「答案頁面」只在講台螢幕顯示，避免被學生看光。

---

### 🛠️ 常見問題排除（Troubleshooting）

| 狀況 | 檢查項目 |
| --- | --- |
| **找不到網頁** | 檢查 IP 是否輸入正確，或伺服器程式是否不小心關閉了。 |
| **手機連不到主機** | 確認手機與主機是否在「同一個」Wi-Fi 名稱下。 |
| **畫面比例不對** | 投影時建議使用 `F11` 全螢幕模式，視覺效果最佳。 |

---
